{
  "name": "nushell-literate-programming",
  "description": "A common space for user and agent in the Nushell terminal — the agent proposes, the user runs, both share the `# =>` dialect. numd (executable markdown), dotnu (scripts that embed their own output), REPL capture, and nu-goodies/claude-nu companions, framed to grow the user's fluency instead of replacing it",
  "version": "0.3.0"
}
